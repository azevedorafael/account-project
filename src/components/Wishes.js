import React, { Component, PropTypes } from 'react';

class Wishes extends Component {
    render() {
        return (
            <div>
                <h1>Your Wishes Informations</h1>

            </div>
        );
    }
}

Wishes.propTypes = {
    // users: PropTypes.array.isRequired
}

export default Wishes;
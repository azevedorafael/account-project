import React, { Component } from 'react';
import PropTypes from "prop-types";

class Orders extends Component {
    render() {
        return (
            <div>
                <h1>Your Orders Informations</h1>

            </div>
        );
    }
}

Orders.propTypes = {
    // users: PropTypes.array.isRequired
}

export default Orders;
import React, { Component, PropTypes } from 'react';

class Address extends Component {
    render() {
        return (
            <div>
                <h1>Your Adress Informations</h1>

            </div>
        );
    }
}

Address.propTypes = {
    // users: PropTypes.array.isRequired
}

export default Address;